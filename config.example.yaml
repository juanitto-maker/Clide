# ============================================
# Clide Configuration File
# ============================================

# ============================================
# 🔑 API Configuration
# ============================================

# Gemini API Key
# ⚠️ SECURITY: For better security, use environment variable instead:
#    export GEMINI_API_KEY="your-key-here"
#    (then leave this commented out)
# gemini_api_key: "YOUR_API_KEY_HERE"

# Gemini Model (default: gemini-2.5-flash)
# Options: gemini-2.5-flash, gemini-1.5-pro, gemini-2.0-flash-exp
gemini_model: "gemini-2.5-flash"

# Temperature (0.0 = focused, 1.0 = creative)
temperature: 0.7

# Maximum tokens per response
max_tokens: 2048

# ============================================
# 📱 Signal Configuration
# ============================================

signal_number: "+1234567890"

# ============================================
# 🤖 Bot Behavior
# ============================================

# Allow bot to execute shell commands
allow_commands: true

# Require user confirmation before running commands
require_confirmation: false

# ============================================
# 🛠️ System Settings
# ============================================

# Log level: trace, debug, info, warn, error
log_level: "info"

# System prompt (defines bot personality)
system_prompt: "You are a helpful AI assistant running on an Android device via Termux. You can help with shell commands, coding, and general tasks."

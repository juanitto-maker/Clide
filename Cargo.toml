[package]
name = "clide"
version = "0.1.0"
edition = "2021"
authors = ["juanitto-maker"]
description = "Terminal-based Gemini AI client for Termux"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dotenvy = "0.15"
colored = "2.0"
